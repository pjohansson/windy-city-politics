#![enable(implicit_some)]
Prefab(
    entities: [
        PrefabEntity(
            data: CharacterPrefab(
                glyph: Glyph('@'),
                variant: PlayerCharacter,
                position: Position(x: 20, y: 10),
            ),
        ),
    ],
)